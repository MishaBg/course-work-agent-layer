services:
  postgres:
    image: postgres:16
    container_name: cw-agent-postgres
    environment:
      POSTGRES_DB: video_agent
      POSTGRES_USER: video_agent
      POSTGRES_PASSWORD: video_agent
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U video_agent -d video_agent"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    container_name: cw-agent-minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  minio-init:
    image: minio/mc:RELEASE.2025-03-12T17-29-24Z
    container_name: cw-agent-minio-init
    depends_on:
      minio:
        condition: service_started
    entrypoint: >
      /bin/sh -c "
      until mc alias set local http://minio:9000 minioadmin minioadmin; do echo 'waiting for minio'; sleep 1; done &&
      mc alias set local http://minio:9000 minioadmin minioadmin &&
      mc mb -p local/video-fragments || true
      "

volumes:
  postgres_data:
  minio_data:
